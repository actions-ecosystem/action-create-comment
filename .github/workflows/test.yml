name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v3.5.1
        with:
          node-version: 16

      - run: yarn install

      - run: yarn test

      - run: yarn format-check

      - run: yarn lint
